# =============================================================================
# Intelligent Duplicate Finder — Python Dependencies
# =============================================================================

# --- GUI Framework ---
PyQt6>=6.6.0

# --- Database / ORM ---
SQLAlchemy>=2.0.0
alembic>=1.13.0

# --- File Operations ---
send2trash>=1.8.0
watchdog>=4.0.0            # Filesystem event monitoring (future use)

# --- Image Processing ---
Pillow>=10.0.0
imagehash>=4.3.0           # Legacy perceptual hashing (fallback)

# --- ML / Embeddings ---
onnxruntime>=1.17.0        # ONNX runtime for model inference
numpy                      # Vectorized array operations (version unpinned to avoid build issues)
transformers>=4.40.0       # Tokenizers for SentenceTransformer ONNX
huggingface-hub>=0.23.0    # Model downloads

# --- Data Processing ---
pandas>=2.0.0              # Optional: dataframe-based reporting

# --- Rust Extension Build ---
maturin>=1.5.0             # Build Rust PyO3 extensions

# --- Logging / Utilities ---
rich>=13.7.0               # Pretty terminal output for CLI
click>=8.1.0               # CLI argument parsing
python-dateutil>=2.8.0

# --- Development / Testing ---
pytest>=8.0.0
pytest-cov>=5.0.0
